[project]
name = "jhub-local-dev"
version = "0.0.0"
description = "Minimal local JupyterHub for exercising the Hub UI / login / spawn flow without Tapis, TAS, LDAP, or Kubernetes"
requires-python = ">=3.12"
dependencies = [
    "jupyterhub>=5.5.0",
    "notebook>=7.0",
    "psycopg2>=2.9.12",
]
